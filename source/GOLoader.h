#pragma once

#include <SDL.h>

struct GOLoader
{
    const char *textureSheet;
    const float xPosition;
    const float yPosition;
    const float width;
    const float height;
    const float mass;
};